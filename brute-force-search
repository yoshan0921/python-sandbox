"""ビット全探索


"""

cards = ['J', 'Q', 'K']
n = len(cards)

print("1 << n", 1 << n)
for bit in range(1 << n):
    print("bit=" + str(bit))
    combination = []
    for i in range(n):
        print("i=" + str(i))
        shift_i = 1 << i
        print("shift_i=" + str(shift_i))
        print("bit & shift_i", bit & shift_i)
        if bit & shift_i > 0:
            combination.append(cards[i])
    print(combination)


# print(2 | 4 | 8)
